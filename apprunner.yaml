version: 1.0
runtime: nodejs20
build:
  commands:
    pre-build:
      - npm ci
    build:
      - npm run build
run:
  command: node server.js
  network:
    port: 3000
    env: APP_PORT
  env:
    - name: NODE_ENV
      value: production
    - name: PORT
      value: "3000"
